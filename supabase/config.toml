project_id = "wcednzaxmxwfiijzmjmx"

[functions.analyze-item]
verify_jwt = false

[functions.ebay-pricing]
verify_jwt = false

[functions.ebay-publish]
verify_jwt = false

[functions.spot-prices]
verify_jwt = false

[functions.transcribe-voice]
verify_jwt = false

[functions.ebay-user]
verify_jwt = false

[functions.ebay-listings]
verify_jwt = false

[functions.create-checkout]
verify_jwt = false

[functions.check-subscription]
verify_jwt = false

[functions.customer-portal]
verify_jwt = false

[functions.ebay-competitor-search]
verify_jwt = false

[functions.category-lookup]
verify_jwt = false

[functions.sync-ebay-taxonomy]
verify_jwt = false

[functions.competitor-prices-cron]
verify_jwt = false

# Cron-only. Gated in code by requireCronSecret, so gateway JWT verification is
# redundant here; leaving verify_jwt at its true default would only mean the anon
# key passes the gateway before the code check rejects it. See RBR-0025.
[functions.cost-alert-cron]
verify_jwt = false

# Cron-only, deletes production storage objects and mutates drafts rows on a
# schedule. Gated in code by requireCronSecret (same reasoning as above).
[functions.cleanup-media-retention]
verify_jwt = false

[functions.stripe-webhook]
# Stripe POSTs here directly — no Supabase JWT, verified via Stripe-Signature header
verify_jwt = false

# NOTE: Supabase CLI cron scheduling support requires CLI v2.x or later.
# This config is commented out to prevent deployment failures with older CLI versions.
# To re-enable, uncomment after upgrading supabase/setup-cli@v1 to a version that
# supports [cron.*] config keys.
#
# [cron.competitor-prices-refresh]
# # Run daily at 2:00 AM UTC
# schedule = "0 2 * * *"
# function = "competitor-prices-cron"